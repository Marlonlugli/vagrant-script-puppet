# Instalar o vim
package { ["vim"]:
 ensure => installed
}

# Instalar o curl
package { ["curl"]:
 ensure => installed
}

# Instalar o telnet
package { ["telnet"]:
 ensure => installed
}

# Instalar o unzip
package { ["unzip"]:
 ensure => installed
}

# Instalar o wget
package { ["wget"]:
 ensure => installed
}

# Instalar o net-tools
package { ["net-tools"]:
 ensure => installed
}

# Instalar o htop
package { ["htop"]:
 ensure => installed
}

# Instalar o nmap
package { ["nmap"]:
 ensure => installed
}
